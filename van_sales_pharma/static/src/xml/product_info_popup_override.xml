<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="van_sales_pharma.ProductInfoPopupOverride" t-inherit="point_of_sale.ProductInfoPopup" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('financials-order')]" position="replace">
            <!-- Removed Financial and Order sections to prevent responsive overlapping on mobile POS -->
        </xpath>
    </t>
</templates>
